
trigger:
- master

pool:
  vmImage: 'Ubuntu 16.04'

steps:
- script: |
    docker build -t $(dockerId).azurecr.io/$(imageName) -f $(System.DefaultWorkingDirectory)/src/ .
    docker login -u $(dockerId) -p $(pswd) $(dockerId).azurecr.io
    docker push $(dockerId).azurecr.io/$(imageName)